# Remote indexing implementation plan

1. Add bind/address and bearer-token contracts with focused HTTP tests.
2. Add authenticated artifact upload/import and cache invalidation.
3. Add the local publisher helper and operator documentation.
4. Run focused tests, full tests, build, inspect the diff, commit, and push.
